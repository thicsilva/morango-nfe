<%= form_for @categ_payment, :html => { :class => "form-horizontal categ_payment" } do |f| %>

  <% if @categ_payment.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@categ_payment.errors.count, "error") %> prohibited this categ_payment from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @categ_payment.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label 'nome da categoria', :class => 'control-label col-lg-2' %>
    <div class="col-lg-5">
      <%= f.text_field :name, :class => 'form-control' %>
    </div>
    <%=f.error_span(:name) %>
  </div>

  <div class="form-group">
    <div class="col-lg-offset-2 col-lg-10">
      <%= f.submit 'Salvar', :class => 'btn btn-primary' %>
      <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                categ_payments_path, :class => 'btn btn-default' %>
    </div>
  </div>

<% end %>
